# 任务：应用顶级标签页与缩放抗乱序

## 1. 顶级标签栏与布局

- [x] 1.1 在 `App.jsx` 根布局最左侧新增竖向顶级标签栏组件（或内联结构），固定宽度（如 56–72px），包含三个标签：提示词管理、MCP 中心、提示词市场（图标 + tooltip 或短文案）。
- [x] 1.2 引入顶级 tab 状态（如 `appTopTab: 'prompts' | 'mcp' | 'market'`），点击标签时更新状态并切换主内容区。
- [x] 1.3 当 `appTopTab === 'prompts'` 时，主内容区渲染现有「可拖拽侧栏 + 主内容」（TemplatesSidebar + 发现/编辑）；当为 `mcp` 时渲染 `McpManager` 全内容；当为 `market` 时渲染 `MarketView` 全内容。
- [x] 1.4 将 MCP 中心、提示词市场从全屏弹窗改为上述主内容区嵌入展示；移除或替换 TemplatesSidebar 顶部的 MCP/市场按钮为“切换至对应顶级标签”的快捷方式（保证功能不丢失）。

## 2. 缩放抗乱序

- [x] 2.1 为顶级标签栏设置最小宽度与按钮最小点击区域（如 44px），标签图标/文字使用不小于约定值的字号或 `clamp`。
- [x] 2.2 为 TemplatesSidebar 标题区与主要按钮、编辑器工具栏主要控件设置 `min-height`/`min-width` 或最小字号，避免缩放后严重挤压。
- [x] 2.3 为主列表项（如模板名称、市场卡片标题）设置合理的最小字号或行高，避免缩放后换行错乱；可在 `index.css` 或组件内统一若干 class。

## 3. 验证

- [x] 3.1 手动验证：切换三个顶级标签，确认提示词管理、MCP 中心、提示词市场功能与当前一致。
- [x] 3.2 手动验证：将浏览器缩放至 80%、90%、110%、125%，检查标签栏、侧栏、工具栏无严重乱序或不可点击。
- [x] 3.3 运行 `openspec validate add-app-top-tabs-and-zoom-resilience --strict --no-interactive`，确保通过。
